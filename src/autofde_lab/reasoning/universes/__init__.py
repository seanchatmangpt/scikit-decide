"""K8s fault-taxonomy universe fixtures.

``k8s_fault_universes.py`` in this package is manufactured by ``ggen sync
run`` from ``ontology/k8s-fault-taxonomy.ttl``. This ``__init__.py`` is
hand-written and intentionally does not re-export from the manufactured
module, matching the precedent in ``autofde_lab.constitution`` -- import
from the specific module you need (``from
autofde_lab.reasoning.universes.k8s_fault_universes import
ALL_UNIVERSES``).

Additive only; not wired into any existing runtime code path.
"""
